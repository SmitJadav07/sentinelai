services:
  - type: web
    name: sentinelai-backend
    env: docker
    dockerfilePath: backend/Dockerfile
    dockerContext: backend
    plan: free
    envVars:
      - key: LOOP_ENABLED
        value: "false"
